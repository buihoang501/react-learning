Forwarding refs in React using forwardRef
The technique is called ref forwarding. Ref forwarding is a technique for 
automatically passing a ref through a component to one of its children.
It's very useful when building reusable component libraries. forwardRef is 
a function used to pass the ref to child component.